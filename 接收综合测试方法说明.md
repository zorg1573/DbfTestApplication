# 接收综合测试方法说明（`RunCombinedReceiveTestsAsync`）

---

## 1. 测试目的与覆盖指标

一次射频扫频，在每个射频频点上依次完成以下 5 项，并写入对应通道的 Excel 工作表 `测试结果N`（N = 通道号 1～8）：

| 指标 | Excel 列 | 单位（写入格式） |
|------|----------|------------------|
| 射频频点 | A（列1） | GHz |
| 接收增益 | B（列2） | dB，保留 2 位小数 |
| -1dB 带宽 | F（列6） | MHz，保留 2 位小数 |
| 镜频抑制 | G（列7） | dB，保留 2 位小数 |
| 带外抑制 | H（列8） | dB，保留 2 位小数 |
| 平坦度 | I（列9） | dB，保留 2 位小数 |

单点测量顺序（同一射频设置下）：

**增益 → 平坦度 → 带外抑制 → -1dB 带宽 → 镜频抑制**

---

## 2. 仪器与链路角色

| 角色 | 设备地址变量 | 用途 |
|------|----------------|------|
| 射频激励（RF） | `_vnaAddress`（矢网） | 加载状态文件 `40.csa`，设功率与中心频率，打开输出 |
| 本振（LO） | `_xinhaoAddress`（信号发生器） | 设本振功率与频率，打开射频输出 |
| 中频测量 | `_pinpuAddress`（频谱仪） | SA 模式读 Marker 功率 |

被测件（DUT）按接收通道加电，并通过 UDP 下发接收测试配置（`ChargeRecievePowerON` → `RecieveTestUDP`）。

### 2.1 功率与端口

- **矢网射频功率**：界面 `textBox_rf_power`（默认约 -56 dBm），指令为按端口设置：`SOUR:POWn <power>`，当前端口号常量 `_vnaRfPortNum = 1`。
- **本振功率**：界面 `textBox_benzhen_power`（默认约 3.7 dBm），指令 `AMPL`。
- **线损/补偿**：界面 `jsbc_textBox`（接收补偿，默认 56），仅用于**增益**计算。

### 2.2 频谱仪初始设置

- 模式：`:INST:SEL SA`
- 状态文件：`/usrdata/Data/kudbf40w.state`
- 扫宽：`START = 75 MHz`，`STOP = 275 MHz`（即中频 175 MHz ± 100 MHz）
- 中心：175 MHz；Marker1 开启并置于 175 MHz

---

## 3. 公共扫频与变频关系

射频扫频范围、点数来自测试集配置：`_startFreq`、`_stopFreq`、`_pointCount`（等间隔）。

对第 i 个频点：

```
f_RF = f_start + i * (f_stop - f_start) / (N - 1)
```

本振与中频关系（下变频到固定中频）：

| 量 | 设定值 |
|----|--------|
| 中频 f_IF | **175 MHz**（固定） |
| 本振 f_LO | `f_RF - 175 MHz` |
| 频谱仪 Marker 读数点（有用信号） | **175 MHz** |

设频后软件延时：RF 200 ms、LO 200 ms，再读中心功率。

记中心功率为：

```
P0 = Marker 在 175 MHz 处读到的功率（dBm）
```

若 P0 无效（读失败），该频点五项全部写空并跳过。

---

## 4. 各指标测试方法

以下均在 **RF = f_RF，LO = f_RF - 175 MHz** 已设定、且已读到有效 P0 的前提下进行（镜频除外会临时改 RF）。

### 4.1 接收增益

**方法**

1. Marker 置于 175 MHz，读取中频功率 P0（dBm）。
2. 加上界面补偿 `jsbc`：

```
增益 G = P0 + jsbc
```

**写入**：增益列，单位按 dB 理解（P0 为 dBm，补偿为线损/校准偏移，需与工程师约定补偿含义）。

**注意**：未在软件内再减矢网输出功率；若规范定义为 `G = P_IF - P_RF + …`，需确认 `jsbc` 是否已包含 P_RF 等项。

---

### 4.2 平坦度

**定义（软件实现）**：在中频 **175 MHz ± 25 MHz**（即 150～200 MHz）内，以 **0.1 MHz** 步进移动 Marker，计算相对中心功率差值绝对值的最大值：

```
平坦度 = max |P0 - P(f)|
         其中 f 取遍 [150 MHz, 200 MHz]，且跳过中心点 175 MHz
```

（实现上跳过与中心相差小于约 0.01 MHz 的点，即跳过中心点本身。）

**单位**：dB。

**说明**：这是「相对 175 MHz 的最大起伏」，不是峰峰值（max − min）。若规范要求峰峰值，需改算法。

---

### 4.3 带外抑制

**定义（软件实现）**：分两段扫描，每段取相对 P0 的最大 `|P0 - P(f)|`，再取两段中的较大者。

| 频段 | 范围 | 步进 |
|------|------|------|
| 低频带外 | 140～150 MHz | 0.1 MHz |
| 高频带外 | 200～210 MHz | 0.1 MHz |

```
D_low  = max |P0 - P(f)|，f 取遍 [140 MHz, 150 MHz]
D_high = max |P0 - P(f)|，f 取遍 [200 MHz, 210 MHz]

带外抑制 OOB = max(D_low, D_high)
```

**单位**：dB。

**说明**：

- 扫描时 RF/LO **不变**，仅移动频谱仪 Marker；反映的是中频滤波器带外相对中心的衰减幅度（以绝对差表示）。
- 若规范定义为「带外功率相对带内的抑制比」且符号/方向有约定（例如只关心 `P0 - P(f)` 且要求为正），请核对是否应改为有符号差值或最小值抑制。

---

### 4.4 -1dB 带宽

**定义（软件实现）**：在频谱仪当前扫宽（75～275 MHz）内，从 175 MHz 分别向低频、高频以 **0.1 MHz** 步进移动 Marker，寻找功率**首次**满足：

```
P(f) ≤ P0 - 1 dB
```

的频点，记为 f_L（低频侧）、f_H（高频侧）。

```
-1dB 带宽(MHz) = (f_H - f_L) / 1e6
```

若某一侧在扫宽内找不到，则该频点带宽写空。

**注意**：

- 判据为「首次 ≤ P0 − 1」，不是插值到精确 -1 dB 点；步进 0.1 MHz，带宽分辨率约 0.1 MHz 量级。
- 未改 RF/LO，仅扫中频 Marker，对应中频通带 -1 dB 带宽。

---

### 4.5 镜频抑制

**变频关系**：中频 175 MHz 时，镜频相对有用射频偏移 `2 × f_IF = 350 MHz`。

**方法**

1. 保持 LO = `f_RF - 175 MHz` 不变；Marker 仍置于 **175 MHz**。
2. 将矢网 RF 中心改为：

```
f_RF_image = f_RF - 350 MHz
```

3. 延时 300 ms 后读 Marker 功率 P_image。
4. 计算：

```
镜频抑制 = P_image - P0   （单位：dB）
```

**说明**：

- 结果一般为负值（镜频处功率更低）；数值越负表示抑制越好。若规范要求正数「抑制量」`P0 - P_image`，符号需与规范统一。
- 本振未改到镜频对应本振；依赖混频器/滤波器对镜频通路的抑制，在 IF=175 MHz 处读出泄漏功率。

---

## 5. 辅助算法摘要

### 5.1 `MeasureMaxPowerDifferenceInMarkerSpanAsync`

在区间 `[f_min, f_max]` 内按步进移动 Marker，返回 `max |P0 - P(f)|`（跳过中心点）。

用于：**平坦度**、**带外抑制**。

### 5.2 `FindMinus1dBMarkerFrequencyAsync`

从中心向一侧步进，返回首次满足 `P(f) ≤ P0 - 1` 的频率；越界则失败。

用于：**-1dB 带宽**。

---

## 6. 关键参数一览（便于核对）

| 参数 | 当前软件取值 |
|------|----------------|
| 中频 | 175 MHz |
| 本振 | `f_RF - 175 MHz` |
| 频谱扫宽 | 75～275 MHz |
| Marker 步进（平坦度/带外/-1dB） | 0.1 MHz |
| 平坦度窗口 | 175 ± 25 MHz |
| 带外低频窗 | 140～150 MHz |
| 带外高频窗 | 200～210 MHz |
| 镜频 RF 偏移 | `f_RF - 350 MHz` |
| 矢网状态文件 | `40.csa` |
| 频谱状态文件 | `/usrdata/Data/kudbf40w.state` |
| 矢网功率端口 | Port 1（`_vnaRfPortNum`） |

---
