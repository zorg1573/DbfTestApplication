# 收发测试指标流程

## 通用前置条件

1. 选择测试类型
2. 勾选待测通道（通道1～通道8，单通道）
3. 填写测试人员
4. 确认频率范围（起始频率、终止频率）与采集点数已配置

---

# 一、接收测试（八指标）

## 通用设备与状态

| 设备 | 用途 |
|------|------|
| 信号源 | 注入射频激励信号 |
| 本振源 | 提供本振，中心频率相对射频偏置 175 MHz |
| 频谱仪 | 读取中频 Marker 功率（中心 175 MHz） |

| 状态文件 | 用途 |
|----------|------|
| `dbfzhupu.dfl` | 频谱仪主测状态（增益/压缩点/镜频/带外/衰减/MGC/平坦度） |
| `dbfzaosheng.dfl` | 频谱仪噪声测试状态 |
| `xinhaoyuan.csa` | 本振源状态 |

---

## 指标一：接收增益

### 流程

1. 写入测试人员信息到 Excel
2. 接收加电 → FPGA 下发接收测试包 → 延时稳定
3. 连接信号源、本振源、频谱仪
4. 加载频谱仪状态，设置 Marker1 于 175 MHz；信号源功率置 -60 dBm 并开输出；本振开输出
5. 在起始～终止频率内按点数扫频：
   - 设置信号源频率
   - 本振频率 = 射频频率 − 175 MHz
   - 读取 Marker 功率，叠加接收补偿后得到增益
6. 将频率写入 `测试结果{通道号}` 第 1 列，增益写入第 2 列
7. 关闭输出、断开设备、关 FPGA、关电源

---

## 指标二：噪声系数

### 流程

1. 选择通道（通道1～8）
2. 连接频谱仪、本振源
3. 加载本振状态并开输出；加载噪声状态文件
4. 按频率点扫频：
   - 本振频率 = 射频频率 − 175 MHz
   - 读取对应频率点噪声系数
5. 按频率匹配写入 Excel「测试结果」噪声相关列
6. 关本振、断开设备、关电源、关 FPGA

---

## 指标三：压缩点

### 流程

1. 选择通道 → 接收加电 → FPGA 发包 → 延时稳定
2. 连接信号源、本振源、频谱仪，加载主测状态，Marker1 置于 175 MHz
3. 信号源初功率 -60 dBm，本振开输出

**阶段 A：参考增益**

4. 各频点：信号源 -60 dBm，记录 Marker 功率作为参考增益

**阶段 B：查找 1 dB 压缩点**

5. 各频点功率从约 -60 dBm 步进至 -50 dBm（步进 0.5 dB）
6. 当「输入功率+补偿 −（当前功率−参考增益）」大于 1 dB 时，判定为压缩点并记录

**阶段 C：定点功率**

7. 各频点信号源置 -50 dBm，记录 Marker 功率（Pset）

8. 压缩点写入 `测试结果{通道号}` 第 4 列，Pset 写入第 5 列
9. 断开设备、关电源、关 FPGA

---

## 指标四：镜频抑制

### 流程

1. 选择通道 → 接收加电 → FPGA 发包 → 延时稳定
2. 连接设备，加载主测状态，信号源 -60 dBm

**阶段 A：带内参考**

3. 各频点按正常射频频率激励，记录 Marker 功率为参考

**阶段 B：镜频激励**

4. 各频点信号源频率改为「射频频率 − 350 MHz」，再次读取 Marker 功率
5. 镜频抑制 = 镜频功率 − 带内参考功率

6. 结果写入 `测试结果{通道号}` 第 6 列
7. 断开设备、关电源、关 FPGA

---

## 指标五：带外抑制

### 流程

1. 选择通道 → 接收加电 → FPGA 发包 → 延时稳定
2. 连接设备，加载主测状态
3. 开启三个 Marker：
   - Marker1：175 MHz（中心）
   - Marker2：175 MHz − 45 MHz
   - Marker3：175 MHz + 45 MHz
4. 信号源 -60 dBm，本振开输出
5. 各频点扫频，分别读取三个 Marker 功率
6. 带外抑制 = min(Marker2, Marker3) − Marker1
7. 结果写入 `测试结果{通道号}` 第 9 列
8. 断开设备、关电源、关 FPGA

---

## 指标六：衰减精度（衰减误差）

### 流程

1. 选择通道 → 写入测试人员 → 接收加电
2. 连接信号源、本振源、频谱仪，加载主测状态
3. 频谱仪扫宽 175 MHz ± 100 MHz，Marker1 于 175 MHz
4. 信号源 -60 dBm，本振开输出
5. 衰减档位从 0～63 共 64 档循环：
   - 将档位编码为衰减控制位，组包下发至被测件
   - 各频点扫频，读取 Marker 功率（必要时做峰值稳定判断）
6. 各档位各频点功率写入 `接收通道衰减精度测试结果{通道号}`
7. 相对标准档位做差值处理，得到衰减精度结果
8. 关输出、断开设备、关 FPGA、关电源

---

## 指标七：MGC

### 流程

1. 选择通道 → 接收加电 → FPGA 发包 → 延时稳定
2. 连接设备，加载主测状态，信号源 -60 dBm

**阶段 A：默认衰减参考**

3. 各频点记录 Marker 功率为参考

**阶段 B：切换默认衰减码**

4. 下发默认衰减控制包
5. 各频点再次读取 Marker 功率
6. MGC = 参考功率 − 切换后功率 − 20

7. 结果写入 `测试结果{通道号}` 第 10 列
8. 断开设备、关电源、关 FPGA

---

## 指标八：平坦度

### 流程

1. 选择通道 → 接收加电 → FPGA 发包 → 延时稳定
2. 连接设备，加载主测状态
3. 开启五个 Marker：
   - Marker1：175 MHz
   - Marker2 / Marker3：±10 MHz
   - Marker4 / Marker5：±20 MHz
4. 信号源 -60 dBm，本振开输出
5. 各频点依次在中心、±10 MHz、±20 MHz 频率激励并读取对应 Marker：
   - ±20 MHz 平坦度 = 中心功率 − min(±10 MHz 两侧功率)
   - ±40 MHz 平坦度 = 中心功率 − min(±20 MHz 两侧功率)
6. ±20 MHz 结果写入第 7 列，±40 MHz 结果写入第 8 列
7. 断开设备、关电源、关 FPGA

---

# 二、发射测试（三指标）

## 通用设备与状态

| 设备 | 用途 |
|------|------|
| 矢网 / 信号源 | 提供发射激励或测相 |
| 功率计 | 读取脉冲峰值功率、顶降、上升/下降沿 |
| 频谱仪 | 读取主谱与带外杂散功率 |

| 状态文件 | 用途 |
|----------|------|
| `40.csa` | 发射功率测试矢网状态 |
| `yixiang.csa` | 移相精度测试矢网状态 |
| `xinhaoyuan.csa` | 信号源状态 |
| `zhupu.dfl` | 频谱仪主谱测量状态 |
| 带外抑制状态文件 | 频谱仪带外杂散测量状态 |

---

## 指标九：发射功率与效率

（含峰值功率、效率、顶降、上升沿、下降沿）

### 流程

1. 写入测试人员信息到 Excel
2. 发射加电
3. 关 FPGA 后测电流 I_DQ5；再下发接收相关包后测电流 I_R5（用于效率分母）
4. 加载功率计状态 → FPGA 下发发射测试包 → 延时稳定
5. 连接矢网与功率计；矢网加载 `40.csa` 并开输出
6. 在起始～终止频率内按点数扫频：
   - 设置矢网中心频率
   - 设置功率计测量频率并触发测量
   - 读取脉冲功率数组：峰值功率、顶降
   - 读取上升沿时长、下降沿时长
   - 读取发射相关电流 I_T85、I_T5
   - 由峰值功率（换算为瓦）与供电功率计算效率
7. 按频率匹配写入 `测试结果{通道号}`：
   - 第 12 列：峰值功率
   - 第 13 列：效率
   - 第 14 列：上升沿
   - 第 15 列：下降沿
   - 第 16 列：顶降
8. 关输出、断开设备、关 FPGA、关电源

---

## 指标十：移相精度

### 流程

1. 写入测试人员信息到 Excel
2. 发射加电
3. 连接矢网，加载 `yixiang.csa` 并开输出
4. 移相档位从 0～63 共 64 档循环：
   - FPGA 下发对应移相控制码
   - 矢网扫描一次，读取相位数据
   - 对相邻档位做相位解缠（跨 ±180° 时补偿 ±360°）
5. 各档位相位写入 `发射通道相移精度测试结果`
6. 相对标准相位做差值，并计算相移精度写入对应通道结果
7. 关输出、断开设备、关 FPGA、关电源

---

## 指标十一：杂散抑制（发射抑制）

### 流程

1. 选择通道 → 发射加电 → FPGA 下发发射测试包 → 延时稳定
2. 连接信号源与频谱仪；信号源加载状态并开输出

**阶段 A：主谱功率**

3. 加载频谱仪主谱状态 `zhupu.dfl`
4. 各频点：设置信号源频率，频谱仪扫宽为中心频率 ±0.2 GHz
5. Marker1 寻峰，记录有效主谱功率

**阶段 B：带外杂散功率**

6. 加载带外抑制状态文件
7. 各频点开启 Marker1 / Marker2，分别置于中心频率 ±300 MHz
8. 取两侧 Marker 功率较大值作为带外杂散功率

**阶段 C：计算抑制**

9. 杂散抑制 = 主谱功率 − 带外杂散功率
10. 按频率匹配写入 `测试结果{通道号}` 第 17 列
11. 关输出、断开设备、关电源

---

# 三、Excel 结果列对照（`测试结果{通道号}`）

| 列 | 内容 | 归属 |
|----|------|------|
| 1 | 频率 | 共用 |
| 2 | 接收增益 | 接收 |
| 4 | 压缩点 | 接收 |
| 5 | Pset（-50 dBm 定点功率） | 接收 |
| 6 | 镜频抑制 | 接收 |
| 7 | 平坦度（±20 MHz） | 接收 |
| 8 | 平坦度（±40 MHz） | 接收 |
| 9 | 带外抑制（±45 MHz） | 接收 |
| 10 | MGC | 接收 |
| 12 | 发射峰值功率 | 发射 |
| 13 | 发射效率 | 发射 |
| 14 | 上升沿 | 发射 |
| 15 | 下降沿 | 发射 |
| 16 | 顶降 | 发射 |
| 17 | 杂散抑制 | 发射 |

> 噪声系数按频率匹配写入「测试结果」表。  
> 衰减精度写入独立工作表 `接收通道衰减精度测试结果{通道号}`。  
> 移相精度写入独立工作表 `发射通道相移精度测试结果`，再汇总到对应通道结果。

---

# 四、建议测试顺序

## 接收

1. 接收增益  
2. 噪声系数  
3. 压缩点  
4. 镜频抑制  
5. 带外抑制  
6. 衰减精度  
7. MGC  
8. 平坦度  

## 发射

9. 发射功率与效率  
10. 移相精度  
11. 杂散抑制  
